package usecases.remove_group_member;

// Output Boundary for Remove Group Member use case.

public interface RemoveGroupMemberOutputBoundary {
    void setPassViewData(RemoveGroupMemberOutputData outputData);
}
